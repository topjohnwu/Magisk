[package]
name = "magiskpolicy"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]
path = "lib.rs"

[build-dependencies]
cxx-gen = { workspace = true }

[dependencies]
base = { path = "../base" }
cxx = { workspace = true }
